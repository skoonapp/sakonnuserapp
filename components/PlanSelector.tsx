import React from 'react';

/**
 * @deprecated This component is now obsolete as of the automatic plan selection update.
 * The app now automatically selects the best available plan (DT plan > tokens) for the user.
 * This file can be safely removed from the project if it is no longer imported anywhere.
 */
const PlanSelector: React.FC = () => null;

export default PlanSelector;
